- reachability reads the build result's graph, sources and hooks directly and returns a readonly Diagnostic list
  trigger: when resolveGraph wires reachability in src/graph/index.ts
  kind: location
  cause: Graph carries neither source paths nor hooks
  resolution: call reachability(built) with build's result, beside cycles(built)
  artifact: export function reachability(built: Pick<BuildResult, 'graph' | 'sources' | 'hooks'>): readonly Diagnostic[]
  signal: silent
- only non-implicit nodes are reported as unreachable, because the rule speaks of entries; a reached implicit interactive node is still reported at the handler that named it
  trigger: when an implicit registry-only node hangs off an unreachable entry
  kind: pattern
  cause: an implicit node has no entry, and its source path is its first reference
  resolution: check sources[key].implicit before warning
  artifact: source?.implicit === true
  signal: silent
- build sets FlowSummary.start to empty string, so reachability skips the flow instead of warning about every entry
  trigger: when a flow's $start names a step that is neither an entry nor registered
  kind: gotcha
  cause: build already reports unknown-step at flows.<flow>.$start, and warning about every entry on top of it would bury that error
  resolution: reachability returns no diagnostics for a flow whose start is empty; documented in the module TSDoc and covered by a test
  artifact: if (flow.start === '') {
  signal: silent
- cycles takes Pick<BuildResult, 'graph' | 'sources'> and returns readonly Diagnostic[]; it reads sources for the closing handler's path and ignores hooks
  trigger: when reachability or resolveGraph consumes cycles
  kind: location
  cause: Graph carries no source paths
  resolution: call cycles(built) directly with build's result
  artifact: export function cycles(built: Pick<BuildResult, 'graph' | 'sources'>): readonly Diagnostic[]
  signal: silent
- cycles exempts only repeat === true, but the public EdgeTarget type declares repeat?: number, so the only exempting value is a type error for typed flows
  trigger: when a typed host writes a wanted loop with repeat: true
  kind: gotcha
  cause: types.ts was written before the stage fixed repeat: true as the loop marker
  resolution: left types.ts unchanged as out of scope; the public-surface stage or the spec owner should widen EdgeTarget.repeat to true or boolean | number
  artifact: repeat?: number;
  signal: silent
- the DFS descends the first-declared handler before checking later ones, so a self-loop on a node's second handler is found after the loops deeper down its first handler
  trigger: when ordering expected cycle diagnostics for a node with several handlers
  kind: gotcha
  cause: walk order is depth-first in declaration order, not handler order per node
  resolution: expect cycle diagnostics in DFS order; the test states why
  artifact: cycle flows.next.build.on.fail
  signal: loud
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: function isPlainObject(value: unknown): value is Readonly<Record<string, unknown>>
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: repeat?: number;
- FlowSummary.start is empty string because the type requires a string
  trigger: when a flow has no step entry and no resolvable $start
  kind: gotcha
  cause: FlowSummary.start is typed string, not optional
  resolution: documented in the build.ts TSDoc; reachability should treat an empty start as no start
  artifact: start: ''
  signal: silent
- build returns sources (path and implicit flag per node key) and hooks (flow, node, anchor, position) beside the graph; when hooks are not edges
  trigger: when reachability or cycles read build output
  kind: location
  cause: Graph has no field for a when placement or for the path an entry was written at
  resolution: read BuildResult.sources for diagnostic paths and BuildResult.hooks for when anchors
  artifact: export interface GraphHook
  signal: silent
- build makes an implicit node for it, as it does for handler targets, so every hook anchor and edge endpoint is a key of graph.nodes
  trigger: when a when anchor names a registered step that has no entry in the flow
  kind: pattern
  cause: the stage text mentions implicit nodes only for handler targets and defines unknown-step only for an anchor that exists nowhere
  resolution: documented in the module TSDoc; the node's NodeSource is implicit true at the when path, and reachability must not report implicit nodes as unreachable entries
  signal: silent
- the substitution silently matched nothing, so the mutation check would have reported a false pass
  trigger: when writing perl mutations containing a dollar key such as flow.$start inside a double-quoted shell string
  kind: gotcha
  cause: shell and perl both interpolate the dollar sign, so the pattern changed before perl saw it
  resolution: compare the mutated file against the backup with cmp and flag MUTATION DID NOT APPLY, then quote the pattern in single quotes
  artifact: MUTATION DID NOT APPLY
  signal: silent
- the mutation survived because the only case that tells the two apart, a non-string step on an entry whose id is itself a registered step, had no test
  trigger: when mutating build.ts to read the spec by node.step instead of the per-entry spec map
  kind: pattern
  cause: an unregistered step name also looks up to undefined, so the simple fixtures could not tell the two implementations apart
  resolution: added a test with build step 42 beside expect and when, which fails under the mutation
  artifact: no specs skip:  37 pass  0 fail
  signal: silent
- GraphEdge.repeat was typed number and could not hold the repeat true that the stage says exempts a back edge
  trigger: when build emits an edge carrying repeat true from an edge object
  kind: gotcha
  cause: the types task typed repeat as a bound count while the spec and stage use repeat true
  resolution: widened GraphEdge.repeat to a required boolean | number and rewrote its TSDoc in the same change
  artifact: Type 'number | boolean' is not assignable to type 'number | undefined'.
  signal: loud
- flatten reports sugar-written inline entries at on.<outcome>, while normalise reports conflicts at the sugar key (onSuccess)
  trigger: when a later step reports a problem inside an inline entry written with sugar
  kind: gotcha
  cause: normalise has already folded sugar into on before flatten runs
  resolution: kept on.<outcome>, which matches the stage's flows.<flow>.<id>.on.<outcome> form
  signal: silent
- reported as duplicate-key at the handler path; the entry the user wrote wins, then the first generated one, and the loser is dropped with its handler
  trigger: when a generated id equals an id already in the flow
  kind: pattern
  cause: DIAGNOSTIC_CODES has no code for a generated-id clash
  resolution: reused duplicate-key and documented it in the module TSDoc
  artifact: duplicate-key
  signal: silent
- flatten keeps repeat as boolean or number because the stage text says repeat: true while types.ts declares repeat?: number
  trigger: when cycles.ts decides whether a back edge is exempt
  kind: gotcha
  cause: the types task and the resolveGraph stage context disagree on the type of repeat
  resolution: left unresolved in flatten; cycles.ts must settle which values count as exempt
  artifact: readonly repeat: boolean | number;
  signal: silent
- flatten leaves string handlers as strings and rewrites only edge objects, so build must read two target shapes: a string id and { to, repeat }
  trigger: when build.ts reads handler targets after flatten
  kind: pattern
  cause: the task text says the handler becomes the id and only edge objects become { to, repeat }
  resolution: build.ts handles both shapes
  signal: silent
- pathToString writes a segment containing a dot in bracket form, not escaped
  trigger: when asserting a diagnostic path whose segment contains a dot
  kind: gotcha
  cause: diagnostics.ts quotes non-identifier segments as ["a.b"]
  resolution: compare the path array instead of its string form for ids containing a dot
  artifact: flows.next["a.b"].on.c
  signal: loud
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: src/graph/types.ts repeat?: number;
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: src/graph/types.ts onChoice?: Handler;
- shape errors are passed through unchanged, not diagnosed, since no diagnostic code covers them
  trigger: when deciding what normalise does with a non-map onChoice or a non-object flow
  kind: pattern
  cause: DIAGNOSTIC_CODES has no shape code besides schema, which belongs to validate
  resolution: documented in the module TSDoc as the host schema's to check
  signal: silent
- EdgeTarget.repeat is a number in types.ts but the stage text says repeat: true exempts a back edge
  trigger: when a later graph stage reads repeat on an edge object
  kind: gotcha
  cause: types task and the resolveGraph stage context disagree on the repeat type
  resolution: left as is; cycles.ts must settle which shape it reads
  artifact: repeat?: number;
  signal: silent
- StepEntry.onChoice is typed Handler, so a spec-shaped onChoice map does not type as StepEntry
  trigger: when typing onChoice as a Record<name, next> in a flow
  kind: gotcha
  cause: src/graph/types.ts declares onChoice?: Handler while the spec says onChoice: Record<name, next>
  resolution: normalise takes Readonly<Record<string, unknown>> instead of Flows
  artifact: onChoice?: Handler;
  signal: silent
- validate throws a TypeError synchronously and never attaches a handler to the returned promise
  trigger: when a Standard Schema validate returns a Promise that later rejects
  kind: gotcha
  cause: the design refuses async results outright, so nothing awaits or catches the promise
  resolution: the tests use only resolving promises; attaching a rejection handler is left as a follow-up decision
  artifact: validated asynchronously; only synchronous validation is supported
  signal: silent
- PIPESTATUS is empty under zsh, so the echoed exit code was blank rather than 0 or 1
  trigger: when reading gate exit codes after a pipe in the zsh shell
  kind: gotcha
  cause: zsh names the array pipestatus in lowercase, and $? after a pipe is the exit code of tail
  resolution: run each gate without a pipe and redirect to /dev/null, then read $?
  artifact: lint exit=
  signal: silent
- ESLint rejects an implicit arrow return that starts on the next line, and the dangling close paren of the call
  trigger: when writing a multi-line arrow body as flatMap(([a, b]) =>\n  expr) in this repo
  kind: gotcha
  cause: the shared rules enforce beside-style arrow bodies and function-paren-newline
  resolution: use a block body with a named local helper and an explicit return
  artifact: Expected no linebreak before this expression  @stylistic/implicit-arrow-linebreak
  signal: loud
- unlabelled entries share one implicit layer, so overlapping keys emit a duplicate-key warning even in the spec's first and third examples
  trigger: when writing merge examples with unlabelled entries that overlap on a key
  kind: gotcha
  cause: same-layer detection treats all entries without $layer as one layer
  resolution: assert only the diagnostic under test, e.g. filter by code, rather than expecting an empty diagnostics array
  artifact: is set more than once within one layer: the unlabelled layer
  signal: loud
- a chain written as `fc\n  .array(` is not matched by a single-line rewrite
  trigger: when regex-renaming fc.x to x across a multi-line chain
  kind: gotcha
  cause: the receiver and method are on different lines
  resolution: fix the leftover `fc` by hand after the rewrite
  artifact: 'fc' is not defined
  signal: loud
- ESLint warns import/no-named-as-default-member for every fc.* call
  trigger: when importing fast-check as a default import in a test
  kind: gotcha
  cause: fast-check exposes named exports, the lint config flags member access on the default import
  resolution: use named imports from 'fast-check'
  artifact: Caution: `fc` also has a named export
  signal: loud
- the quotes rule has no avoidEscape, so a double-quoted string holding single quotes is a lint error
  trigger: when a string in a test contains single quotes
  kind: gotcha
  cause: @stylistic/quotes set to single without avoidEscape
  resolution: escape the inner quotes inside a single-quoted string
  artifact: Strings must use singlequote  @stylistic/quotes
  signal: loud
- Array.prototype.reduce skips the holes of a sparse array, so a missing entry would be skipped silently and indexes would still look correct
  trigger: when iterating the entries array to apply each one
  kind: pattern
  cause: reduce visits only present indices
  resolution: use an index for loop so apply refuses the hole under its position
  artifact: entry 1: expected a plain object, got undefined
  signal: silent
- merge([{a:{x:1,y:2}}, {a:{y:3,z:4}}]) returns the spec value but also one duplicate-key warn at a.y; the $replace example also warns at a
  trigger: when merging the spec's DoD examples with default options
  kind: gotcha
  cause: unlabelled entries share one implicit layer, and a $replace or new map at a path records a set there, so default 'warn' fires
  resolution: assert only the value for those examples, or give the entries distinct $layer or duplicates: 'allow' when a test expects no diagnostics
  artifact: is set more than once within one layer: the unlabelled layer (entries 0, 1)
  signal: loud
- provenance records a set at 'a.x', but the merged value has no nested a.x, so no drop can be found
  trigger: when an entry uses a dotted key such as { 'a.x': 1 }
  kind: gotcha
  cause: provenance paths are dot-joined, the same limit apply and ConfigEntry already document
  resolution: report without an entry, with the message 'absent from the merged value'
  artifact: is required but absent from the merged value
  signal: silent
- a non-map value above a required path also drops it, which the task text (false or $replace only) does not mention
  trigger: when a later entry overwrites a parent key with a scalar or array
  kind: gotcha
  cause: apply records a plain set at the parent, which drops the whole subtree below it
  resolution: name that entry as the dropper, with the message 'set a to a value that is not a map'
  artifact: set a to a value that is not a map
  signal: silent
- a set or replace record at the required path itself can never come after the path's last set, so only false at the path and records at keys above it can drop it
  trigger: when deciding which provenance records drop a required path
  kind: pattern
  cause: apply records set or replace at the path whenever it writes the path, so any such record counts toward the last set
  resolution: filter records to entry > lastSet over the path and its prefixes, and treat every surviving record as a drop
  artifact: 26 pass  0 fail <- self replace counts
  signal: silent
- breaking one rule at a time with sed on the module and rerunning the file showed each rule has a test that can fail
  trigger: when a test file passes on its first run
  kind: pattern
  cause: a suite never seen failing could be a false negative
  resolution: back up to /tmp, sed one rule, run bun test on the file, restore, and diff to confirm
  artifact: entry counted twice          19 pass  1 fail
  signal: silent
- set and replace count as setting and remove does not; a map merged into an existing map records nothing at its own path, so only shared leaves collide
  trigger: when deciding which provenance kinds count as setting a path
  kind: pattern
  cause: apply records set only when it starts a new map, not when it merges into one
  resolution: duplicates filters kind 'remove' and skips the root path ''
  artifact: touch.kind === 'remove'
  signal: silent
