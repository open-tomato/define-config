- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: src/graph/types.ts repeat?: number;
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: src/graph/types.ts onChoice?: Handler;
- shape errors are passed through unchanged, not diagnosed, since no diagnostic code covers them
  trigger: when deciding what normalise does with a non-map onChoice or a non-object flow
  kind: pattern
  cause: DIAGNOSTIC_CODES has no shape code besides schema, which belongs to validate
  resolution: documented in the module TSDoc as the host schema's to check
  signal: silent
- EdgeTarget.repeat is a number in types.ts but the stage text says repeat: true exempts a back edge
  trigger: when a later graph stage reads repeat on an edge object
  kind: gotcha
  cause: types task and the resolveGraph stage context disagree on the repeat type
  resolution: left as is; cycles.ts must settle which shape it reads
  artifact: repeat?: number;
  signal: silent
- StepEntry.onChoice is typed Handler, so a spec-shaped onChoice map does not type as StepEntry
  trigger: when typing onChoice as a Record<name, next> in a flow
  kind: gotcha
  cause: src/graph/types.ts declares onChoice?: Handler while the spec says onChoice: Record<name, next>
  resolution: normalise takes Readonly<Record<string, unknown>> instead of Flows
  artifact: onChoice?: Handler;
  signal: silent
- validate throws a TypeError synchronously and never attaches a handler to the returned promise
  trigger: when a Standard Schema validate returns a Promise that later rejects
  kind: gotcha
  cause: the design refuses async results outright, so nothing awaits or catches the promise
  resolution: the tests use only resolving promises; attaching a rejection handler is left as a follow-up decision
  artifact: validated asynchronously; only synchronous validation is supported
  signal: silent
- PIPESTATUS is empty under zsh, so the echoed exit code was blank rather than 0 or 1
  trigger: when reading gate exit codes after a pipe in the zsh shell
  kind: gotcha
  cause: zsh names the array pipestatus in lowercase, and $? after a pipe is the exit code of tail
  resolution: run each gate without a pipe and redirect to /dev/null, then read $?
  artifact: lint exit=
  signal: silent
- ESLint rejects an implicit arrow return that starts on the next line, and the dangling close paren of the call
  trigger: when writing a multi-line arrow body as flatMap(([a, b]) =>\n  expr) in this repo
  kind: gotcha
  cause: the shared rules enforce beside-style arrow bodies and function-paren-newline
  resolution: use a block body with a named local helper and an explicit return
  artifact: Expected no linebreak before this expression  @stylistic/implicit-arrow-linebreak
  signal: loud
- unlabelled entries share one implicit layer, so overlapping keys emit a duplicate-key warning even in the spec's first and third examples
  trigger: when writing merge examples with unlabelled entries that overlap on a key
  kind: gotcha
  cause: same-layer detection treats all entries without $layer as one layer
  resolution: assert only the diagnostic under test, e.g. filter by code, rather than expecting an empty diagnostics array
  artifact: is set more than once within one layer: the unlabelled layer
  signal: loud
- a chain written as `fc\n  .array(` is not matched by a single-line rewrite
  trigger: when regex-renaming fc.x to x across a multi-line chain
  kind: gotcha
  cause: the receiver and method are on different lines
  resolution: fix the leftover `fc` by hand after the rewrite
  artifact: 'fc' is not defined
  signal: loud
- ESLint warns import/no-named-as-default-member for every fc.* call
  trigger: when importing fast-check as a default import in a test
  kind: gotcha
  cause: fast-check exposes named exports, the lint config flags member access on the default import
  resolution: use named imports from 'fast-check'
  artifact: Caution: `fc` also has a named export
  signal: loud
- the quotes rule has no avoidEscape, so a double-quoted string holding single quotes is a lint error
  trigger: when a string in a test contains single quotes
  kind: gotcha
  cause: @stylistic/quotes set to single without avoidEscape
  resolution: escape the inner quotes inside a single-quoted string
  artifact: Strings must use singlequote  @stylistic/quotes
  signal: loud
- Array.prototype.reduce skips the holes of a sparse array, so a missing entry would be skipped silently and indexes would still look correct
  trigger: when iterating the entries array to apply each one
  kind: pattern
  cause: reduce visits only present indices
  resolution: use an index for loop so apply refuses the hole under its position
  artifact: entry 1: expected a plain object, got undefined
  signal: silent
- merge([{a:{x:1,y:2}}, {a:{y:3,z:4}}]) returns the spec value but also one duplicate-key warn at a.y; the $replace example also warns at a
  trigger: when merging the spec's DoD examples with default options
  kind: gotcha
  cause: unlabelled entries share one implicit layer, and a $replace or new map at a path records a set there, so default 'warn' fires
  resolution: assert only the value for those examples, or give the entries distinct $layer or duplicates: 'allow' when a test expects no diagnostics
  artifact: is set more than once within one layer: the unlabelled layer (entries 0, 1)
  signal: loud
- provenance records a set at 'a.x', but the merged value has no nested a.x, so no drop can be found
  trigger: when an entry uses a dotted key such as { 'a.x': 1 }
  kind: gotcha
  cause: provenance paths are dot-joined, the same limit apply and ConfigEntry already document
  resolution: report without an entry, with the message 'absent from the merged value'
  artifact: is required but absent from the merged value
  signal: silent
- a non-map value above a required path also drops it, which the task text (false or $replace only) does not mention
  trigger: when a later entry overwrites a parent key with a scalar or array
  kind: gotcha
  cause: apply records a plain set at the parent, which drops the whole subtree below it
  resolution: name that entry as the dropper, with the message 'set a to a value that is not a map'
  artifact: set a to a value that is not a map
  signal: silent
- a set or replace record at the required path itself can never come after the path's last set, so only false at the path and records at keys above it can drop it
  trigger: when deciding which provenance records drop a required path
  kind: pattern
  cause: apply records set or replace at the path whenever it writes the path, so any such record counts toward the last set
  resolution: filter records to entry > lastSet over the path and its prefixes, and treat every surviving record as a drop
  artifact: 26 pass  0 fail <- self replace counts
  signal: silent
- breaking one rule at a time with sed on the module and rerunning the file showed each rule has a test that can fail
  trigger: when a test file passes on its first run
  kind: pattern
  cause: a suite never seen failing could be a false negative
  resolution: back up to /tmp, sed one rule, run bun test on the file, restore, and diff to confirm
  artifact: entry counted twice          19 pass  1 fail
  signal: silent
- set and replace count as setting and remove does not; a map merged into an existing map records nothing at its own path, so only shared leaves collide
  trigger: when deciding which provenance kinds count as setting a path
  kind: pattern
  cause: apply records set only when it starts a new map, not when it merges into one
  resolution: duplicates filters kind 'remove' and skips the root path ''
  artifact: touch.kind === 'remove'
  signal: silent
- reduce without an explicit type argument infers unknown for the accumulator; bun test passes but check-types fails
  trigger: when typing a reduce over readonly unknown[] entries into MergeState in a test helper
  kind: gotcha
  cause: the initial value overload is not chosen when the element type is unknown
  resolution: write entries.reduce<MergeState>(...) as apply.test.ts does
  artifact: error TS2571: Object is of type 'unknown'.
  signal: loud
- building each result object with Object.fromEntries defines own properties, so a __proto__ key from JSON.parse stays a key and does not change the prototype
  trigger: when building merged objects from keys found in untrusted entries
  kind: pattern
  cause: assigning result[key] with key __proto__ calls the prototype setter
  resolution: collect keys in a Map and finish with Object.fromEntries; a test checks the prototype and that no pollution leaks
  artifact: Object.fromEntries(result)
  signal: silent
- breaking one rule at a time in a scratch copy of the module (sed on a copy, run the tests, restore from backup) shows each rule has a test that can fail
  trigger: when a test file passes on its first run
  kind: pattern
  cause: a suite that has never been seen failing could be a false negative
  resolution: cp the module to /tmp, sed one rule, run bun test on the file, cp back, and diff to confirm the restore
  artifact: 31 pass  7 fail  <- no $replace
  signal: silent
- provenance keys are dot-joined paths as the spec says, so { 'a.b': 1 } and { a: { b: 1 } } both record under a.b and can be reported as duplicates of each other
  trigger: when a config key itself contains a dot
  kind: gotcha
  cause: a dot-joined string cannot tell a dotted key from a nested one
  resolution: documented on the Provenance type, the same limit types.ts states for Required paths; duplicates.ts will split keys on dots when building diagnostic paths
  artifact: share a dot-joined path with the nested keys they spell
  signal: silent
- the spec rule that false at a key removes it applies to every key at runtime, so a boolean field cannot be set to false: it is deleted
  trigger: when an entry sets a boolean config field to false, such as { verbose: false }
  kind: gotcha
  cause: apply has no schema, so it cannot tell a keyed-map id from a named boolean key; the types allow false for any boolean field
  resolution: implemented as the spec says and documented; the spec owner should decide whether booleans need an escape or whether removal counting as false is acceptable
  artifact: false removes the key
  signal: silent
- the const widens string literals, so an outcome-typed field such as expect becomes string and the accepted twin is refused
  trigger: when building a test entry as a plain const object literal and passing it to defineConfig<T>
  kind: gotcha
  cause: TypeScript widens literal properties of an unannotated object literal
  resolution: annotate the entry as ConfigEntry<T> or inline the literal in the call
  artifact: Property '$replace' is missing in type
  signal: loud
- the index signature must also admit string and boolean for $start and $unattended, so a step id set to a string is not refused
  trigger: when a flow object mixes reserved $-keys with an index signature of step entries
  kind: pattern
  cause: TypeScript checks named properties against the index signature
  resolution: documented in the TSDoc of FlowEntry; an intersection is not an option because it refuses $start itself
  signal: silent
- a non-numeric repeat is refused, but the error names String.prototype.repeat instead of the edge field
  trigger: when an edge type is a union of string and an object with a repeat field
  kind: gotcha
  cause: the object literal is also checked against string's apparent type, which has a repeat method
  resolution: the error still fires and the test stands; read the message with this in mind
  artifact: Type 'string' is not assignable to type 'number | ((count: number) => string) | undefined'.
  signal: silent
- the runtime assertion's own argument is type-checked against the typed value, so the deliberately wrong twin makes toEqual fail check-types
  trigger: when a type test passes an object literal typed from a union of literals to expect().toEqual()
  kind: gotcha
  cause: bun:test expect<T> infers T from the actual value and checks the expected argument against it
  resolution: write expect<unknown[]>([...]) for the runtime companion of a refused line
  artifact: error TS2769: No overload matches this call.
  signal: loud
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: Property '$layer' is incompatible with index signature.
- a mapped type with ? on every key made $replace optional, so a replace shape matched objects without the marker
  trigger: when writing a homomorphic mapped type that needs $replace required and other keys optional
  kind: pattern
  cause: a ? on the mapped type applies to every key, including $replace
  resolution: put the modifiers on the skeleton ({ $replace: 0 } & OptionalKeys<T>) and map over keyof S without modifiers so each key keeps its own
  signal: silent
- strip every directive and require the error count to equal the directive count, then apply targeted loosening mutations and require unused-directive errors
  trigger: when proving @ts-expect-error assertions are not false negatives
  kind: pattern
  cause: a passing check-types alone cannot show that each refused line errors for the intended reason
  resolution: sed out the directives, count error TS lines, restore; perl-edit one rule at a time, run tsc, restore and diff
  artifact: TS2578: Unused '@ts-expect-error' directive.
  signal: silent
- the $replace: true key is checked against the map's index signature and refused
  trigger: when a $replace object stands in for a keyed map (Record<string, V>)
  kind: gotcha
  cause: TS relates every source property, named or not, to the target's string index signature
  resolution: the replace shape's index signature admits true beside the partial value; the resulting gap is documented in TSDoc
  artifact: Property '$replace' is incompatible with index signature.
  signal: loud
- TypeScript accepted a stray true at a map id (steps: { lint: true }) that each union member refuses on its own
  trigger: when typing a keyed-map entry as an intersection of a named-key part and an index-signature part, in a union with a $replace shape
  kind: gotcha
  cause: a union of intersections that carry index signatures is related more loosely than a union of single object types
  resolution: build each shape as one homomorphic mapped type over a key skeleton (OptionalKeys<T> & { [id]?: 0 }) so the result is a single object type
  artifact: { steps: { lint: true } }
  signal: silent
- NOTICE said the package redistributes no third-party source file, and that became false
  trigger: vendoring third-party source
  kind: location
  cause: the same-commit doc rule applies to NOTICE
  resolution: rewrote the sentence and appended the MIT text
  artifact: redistributes no
    third-party source file
  signal: silent
- the implicit-arrow-linebreak rule rejects an arrow body that starts on the next line
  trigger: writing a multi-line ternary as an arrow body in a test
  kind: gotcha
  cause: the arrow-function style is 'beside', so the body must start on the same line as the arrow
  resolution: move the logic into a named function that uses early returns
  artifact: Expected no linebreak before this expression  @stylistic/implicit-arrow-linebreak
  signal: loud
- spec lists 9 codes; plan adds handler-conflict and load-failed (handler-conflict also appears in the spec risks section)
  trigger: when choosing diagnostic codes
  kind: location
  cause: code list spread across spec Diagnostics section, spec risks, and plan context
  resolution: used the plan list of 11 codes
  artifact: .rafa/plans/PLAN-rafa-2-open-tomato-define-config.md
  signal: silent
