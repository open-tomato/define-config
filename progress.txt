- bun test reports 420 pass, not the 398 recorded at 9e09c35
  trigger: comparing the test count with the plan context
  kind: location
  cause: presumably the tests added by earlier tasks in this plan
  resolution: none needed; noted for the close-out count
  artifact: 420 pass
  signal: silent
- fixture .ts files under src/loader/fixtures got declarations emitted into dist
  trigger: running bun run build with only test files excluded
  kind: gotcha
  cause: tsconfig.build.json includes all of src and excluded only *.test.ts
  resolution: exclude src/**/fixtures/** in tsconfig.build.json
  artifact: dist/loader/fixtures/acceptance/project/rafa.config.d.ts
  signal: silent
- a cross-area value import sits in the parent group right after the ../diagnostics import, and lint accepted it without changes
  trigger: when a graph or loader module imports a merge helper
  kind: pattern
  cause: import/order sorts the parent group alphabetically
  resolution: place ../merge/plain-object after ../diagnostics
  artifact: import should occur before import of
  signal: loud
- the build emits a declaration file for an internal module even though src/index.ts does not export it
  trigger: when adding an internal module under src/
  kind: gotcha
  cause: tsconfig.build.json excludes only src/**/*.test.ts, so every non-test source file gets a .d.ts
  resolution: none needed; it is absent from dist/index.d.ts so it is not public API
  artifact: dist/merge/plain-object.d.ts
  signal: silent
- artifact: PLAN_TRACKER-rafa-13-0-1-1.md: A `$replace: true` on the entry itself replaces the whole value merged so far
- the first-character helper (Heads string plus Chars) now exists word for word in both src/types.ts (RootIdHeads) and src/graph/types.ts (StepIdHeads)
  trigger: when adding a $-excluding key set in src/types.ts
  kind: location
  cause: the graph helper is module-private, and sharing it would change src/graph/types.ts outside this task
  resolution: left duplicated; a follow-up can move one copy to a shared internal type and import it from both modules
  artifact: type RootIdHeads = 
  signal: silent
- expect([bad.x, good.x]).toEqual([...]) fails check-types because the array literal infers the declared type and toEqual refuses the bad runtime value
  trigger: when asserting a mix of a correctly and an incorrectly typed value in one expect() in a type test
  kind: gotcha
  cause: bun:test toEqual is typed against the inferred element type of expect's argument
  resolution: write expect<unknown[]>(...) as src/graph/types.test.ts does
  artifact: error TS2769: No overload matches this call.
  signal: loud
- the type refuses empty, $-prefixed and non-ASCII-first root ids and string-typed index reads on a keyed-map root, which merge still accepts
  trigger: when comparing the typed root-id set of LayeredEntry with what merge accepts at run time
  kind: gotcha
  cause: TypeScript has no negated template types, so the exclusion enumerates printable ASCII first characters minus $
  resolution: documented in the LayeredEntry TSDoc and a README paragraph under merge
  artifact: type RootId = `${Chars<RootIdHeads>}${string}`;
  signal: silent
- [K in keyof S as string extends K ? RootId : K] renames the index key while the value can still branch on the original K, and ? modifiers from S are preserved
  trigger: when renaming an index-signature key in a mapped type with an as clause
  kind: pattern
  cause: a mapped type over keyof S with an as clause stays homomorphic for modifiers; the value expression sees the pre-rename key
  resolution: reuse EntryAt unchanged for the value and only swap the key type, so no duplicate value logic is needed
  artifact: type LayeredShape<S, T, Required extends string>
  signal: silent
- the intersection refuses $layer: 'project' because TypeScript checks the named $layer property against the root index signature too
  trigger: when adding $layer beside a keyed-map root via ConfigEntry<T> & { $layer?: string }
  kind: gotcha
  cause: object-literal properties are related to every applicable string index signature of the target, named target properties included
  resolution: build one mapped type and rename the string index key to a template-literal key set that excludes a leading $
  artifact: is not assignable to type 'LayeredEntry<Steps>'
  signal: loud
- the type refuses empty, non-ASCII-first and non-reserved $-prefixed step ids, which the runtime (FLOW_RESERVED in build/flatten/normalise) still treats as step ids
  trigger: when comparing the typed step-id set with what resolveGraph accepts at run time
  kind: gotcha
  cause: the template-literal exclusion can only enumerate ASCII first characters and must exclude all $ keys to keep $start and $unattended off the index
  resolution: documented in the FlowEntry TSDoc and the README; the upside is that a misspelt $strat is now caught (TS2561 with a did-you-mean hint)
  artifact: Did you mean to write '$start'?
  signal: silent
- flow[k] with k: string is now TS7053, because only template-literal index signatures exist and a string key matches none of them
  trigger: when reading a FlowEntry with a key typed only as string
  kind: gotcha
  cause: a bare string is not assignable to any `${Head}${string}` pattern
  resolution: noted in the FlowEntry TSDoc; hosts narrow the key or read through Object.entries, which still type-checks
  artifact: Element implicitly has an 'any' type because expression of type 'string' can't be used to index type 'FlowEntry'.
  signal: loud
- TypeScript has no negated template types; the exclusion has to be written as an enumerated first-character union: [id: `${Chars<Heads>}${string}`]
  trigger: when expressing a key set that excludes a prefix in an index signature
  kind: pattern
  cause: index signature keys may only be string, number, symbol, template-literal patterns, or unions of these
  resolution: enumerate printable ASCII minus $ as a string-literal type and split it with a tail-recursive Chars<S, Acc> conditional type
  artifact: type StepId = `${Chars<StepIdHeads>}${string}`;
  signal: silent
- the correct shape is refused: TypeScript checks every object-literal property against an applicable index signature, named target properties included
  trigger: when typing reserved $ keys as named properties beside a plain string index signature, even inside one homomorphic mapped type
  kind: gotcha
  cause: membersRelatedToIndexer does not skip properties the target also names; a string or boolean value is not assignable to StepEntry
  resolution: type the index key as a template-literal pattern that $start and $unattended do not match, so the index signature never applies to them
  artifact: Property '$start' is incompatible with index signature.
  signal: loud
- The declared onChoice property overrides the [option: string]: unknown index signature, so onChoice: 'x' is rejected; the old type made the directive unused
  trigger: when adding @ts-expect-error for a retyped field that sits beside an index signature of unknown
  kind: pattern
  cause: a named property's own type takes precedence over the index signature
  resolution: check it by putting the old type back and reading tsc: TS2578 on the directive proves it now tests the change
  artifact: error TS2578: Unused '@ts-expect-error' directive.
  signal: silent
- The README sugar table and the normalise.ts TSDoc already described onChoice as a map; only the type and its own TSDoc were wrong
  trigger: when checking README and TSDoc for text that restates the onChoice type
  kind: location
  cause: the 0.1.0 type was out of step with both the runtime and the prose
  resolution: only types.ts and types.test.ts needed edits
  artifact: onChoice: { <name>: id, … }
  signal: silent
- progress.txt was already modified (emptied) in the working tree before the task began
  trigger: when the session starts
  kind: location
  cause: left by an earlier loop step, not by this task
  resolution: left untouched; git add -A will include the deletion
  artifact: progress.txt | 224 -----
  signal: silent
- restoring the old types.ts via git stash and running tsc shows which directives the change made meaningful
  trigger: when adding @ts-expect-error assertions for a narrowed type
  kind: pattern
  cause: an @ts-expect-error that already errored under the old type proves nothing about the narrowing
  resolution: under the old type the 2 and false lines report TS2578 and 'twice' does not, as expected
  artifact: error TS2578: Unused '@ts-expect-error' directive.
  signal: silent
- src/graph/index.test.ts typed a Flows value with repeat: 2, which broke under the narrowed type
  trigger: when narrowing a public type used in typed fixtures of other test files
  kind: gotcha
  cause: the Flows type-acceptance test used a number repeat incidentally
  resolution: changed it to repeat: true; build to test is not a back edge so diagnostics stay empty
  artifact: src/graph/index.test.ts(134,70): error TS2322: Type 'number' is not assignable to type 'true'.
  signal: loud
