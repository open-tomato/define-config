- indexed access yields number | undefined, which fails toBe(number) type checks
  trigger: when writing tests that index result.sources under noUncheckedIndexedAccess
  kind: gotcha
  cause: check-types includes test files with strict index checks; bun test does not check types
  resolution: use ?? fallback or optional chaining before passing to expect
  artifact: Argument of type 'number | undefined' is not assignable to parameter of type 'number'
  signal: loud
- a non-dot temp dir with a tsconfig extending ../tsconfig.json and include [block.ts] type-checks the block against src/index.ts
  trigger: when type-checking a README ts fence by extraction
  kind: pattern
  cause: lint parses README fences but does not type-check them
  resolution: rewrite the package specifier to ../src/index, run bunx tsc -p, and plant an error as a control
  artifact: readmecheck/tsconfig.json
  signal: silent
- src/loader/read.ts imports Source from ./lookup for readSource and checkSource
  trigger: when splitting the public Source from the lookup-internal type
  kind: location
  cause: readSource takes the found source before any range exists
  resolution: renamed the lookup type to FoundSource and updated read.ts's type-only import and two annotations
  artifact: import type { Source } from './lookup';
  signal: loud
- the sources toEqual in src/loader/acceptance.test.ts also breaks, beyond the loader index.test.ts assertions the plan listed
  trigger: when adding entries to Source of LoadResult.sources
  kind: gotcha
  cause: the acceptance test asserts result.sources whole
  resolution: added entries [0, 1] and [1, 2] there as the minimal repair; the next task extends the file
  artifact: Property 'entries' is missing in type '{ layer: string; path: string; }' but required in type 'Source'.
  signal: loud
- an untyped const array of records widens kind to string, so check-types fails while bun test passes
  trigger: when writing an expected-records array in a test with kind literals
  kind: gotcha
  cause: bun test strips types without checking them, and the literal widens without an annotation
  resolution: annotate the array as ProvenanceRecord[] (import type from ./apply)
  artifact: Type 'string' is not assignable to type 'ProvenanceKind'.
  signal: loud
- src/index.test.ts pins the function count in a test title as well as in FUNCTIONS
  trigger: when adding a public function to src/index.ts
  kind: location
  cause: the title 'exports exactly the six functions' names the count in words
  resolution: update the title along with the FUNCTIONS list
  artifact: exports exactly the six functions
  signal: silent
- check-types rejects the fixture while bun test passes
  trigger: when a test compares provenance records against an untyped object-literal fixture
  kind: gotcha
  cause: a literal widens kind to string, not ProvenanceKind, and bun's toEqual is typed against the received type
  resolution: annotate the fixture as readonly ProvenanceRecord[]
  artifact: Type 'string' is not assignable to type 'ProvenanceKind'.
  signal: loud
- Object.freeze on a Map does not stop Map.prototype.set
  trigger: when freezing a ReadonlyMap returned from a public API
  kind: gotcha
  cause: freeze locks object properties, not a Map's internal entries
  resolution: freeze the map, its arrays and its records, and say in TSDoc that only a write casting away ReadonlyMap can change the result
  artifact: Object.freeze(copy)
  signal: silent
- a test that two merge() calls return different map objects passes even without the copy
  trigger: when asserting that a returned provenance map is a copy
  kind: gotcha
  cause: apply already builds a new Map on every merge, so identity tells you nothing about copying
  resolution: prove the copy through freezing: the freeze test fails when the frozen copy is removed
  artifact: expect(merge(entries).provenance).not.toBe(merge(entries).provenance)
  signal: silent
