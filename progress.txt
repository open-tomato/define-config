- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: Typed config arrays with id-keyed merge
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: extends: ['markdown/recommended']
- extract each fenced ts block, rewrite the package import to ../src/index, run tsc with a tsconfig extending the repo one, then execute each block and compare output to the comments
  trigger: when verifying README examples match shipped signatures
  kind: pattern
  cause: README code is neither type-checked nor, in this repo, linted by any gate
  resolution: one-off harness in a temporary folder, deleted after use
  artifact: readme-check-tmp
  signal: silent
- tsc include globs skip dot-directories, so a temporary harness under .readme-check found no inputs
  trigger: when running tsc over files in a dot-directory
  kind: gotcha
  cause: TypeScript include wildcards exclude names starting with a dot
  resolution: used a non-dot temporary directory and removed it after
  artifact: error TS18003: No inputs were found in config file
  signal: loud
- MergeResult.value TSDoc said the value shares no object with any entry, but class instances and functions pass through by reference
  trigger: when auditing merge immutability claims
  kind: gotcha
  cause: copyValue copies only arrays and plain objects by design
  resolution: doc corrected on MergeResult.value, apply and the README; behaviour unchanged
  artifact: a new object sharing no object or array with any
  signal: silent
- the public EdgeTarget.repeat was typed number while cycles only exempts repeat === true, so the documented loop mark did not type-check and repeat: 2 type-checked but still yields cycle
  trigger: when type-checking the README repeat: true loop example
  kind: gotcha
  cause: graph/types.ts typed repeat as a count while flatten and GraphEdge carry boolean | number
  resolution: widened EdgeTarget.repeat to boolean | number with TSDoc saying only true marks the loop; type test updated
  artifact: Type 'boolean' is not assignable to type 'number'.
  signal: loud
- bun run lint does not lint code inside README fenced ts blocks; identical code gives 4 lint errors as a .ts file and 0 inside a README.md ts block
  trigger: when checking README examples with bun run lint
  kind: gotcha
  cause: eslint.config.mjs applies markdown/recommended to **/*.md but sets no processor: 'markdown/markdown', so fenced code is never extracted
  resolution: verified README examples by extracting each ts block, running tsc against src/index.ts, and executing them; lint config left unchanged
  artifact: extends: ['markdown/recommended']
  signal: silent
- several types used in public signatures are not in the stage's type list and are not exported: ResolveGraphResult, ConfigLoader, SchemaSection, DiagnosticLevel, Loaders, LayerDir, Source
  trigger: when deciding which types src/index.ts exports
  kind: location
  cause: the stage lists 15 types explicitly; I kept to that list rather than widen the surface
  resolution: left for the review task to decide; users can still reach them through ReturnType or Parameters
  artifact: ResolveGraphResult
  signal: silent
- the first match of an export name in index.d.ts is inside the TSDoc comment, not the export clause
  trigger: when a control test strips a function name from dist/index.d.ts with a regex
  kind: pattern
  cause: the index module docblock links every function with {@link name}
  resolution: the control replaces the literal clause text '{ validate, validateSections }' instead of the first occurrence of the name
  artifact: {@link validateSections}
  signal: silent
- neither --ignore-dce-annotations nor rewriting the entry as import-then-export stops the pruning
  trigger: when trying to work around the bun sideEffects pruning without touching package.json
  kind: gotcha
  cause: bun still reads the package-level sideEffects field for the entry file
  resolution: changed the manifest field instead; measured each alternative at 1 module and 100 bytes
  artifact: Bundled 1 module
  signal: loud
- bun build 1.3.14 bundles only the entry and emits an export list naming bindings it never defines; the build exits 0
  trigger: when bun build bundles an entry that only re-exports, inside a package whose package.json sets sideEffects false
  kind: gotcha
  cause: bun applies the package's sideEffects false to the build entry itself and prunes every re-exported module
  resolution: set sideEffects to the unpublished entry path src/index.ts so the entry is kept; the published dist files remain side-effect-free
  artifact: Export 'createLoader' is not defined in module
  signal: silent
- the root .gitignore rule .rafa/ ignores the fixture's .rafa/config.yaml, so git add -A would silently skip it
  trigger: when adding a fixture directory named .rafa under src/
  kind: gotcha
  cause: rafa's tracking block ignores every directory named .rafa at any depth
  resolution: added !src/loader/fixtures/**/.rafa/ after the managed block in .gitignore
  artifact: .rafa/
  signal: silent
- import/order wants import type from '.' before the parent '../' type imports, and a blank line between parent and sibling value-import groups
  trigger: when a test file in src/<area>/ imports its own index module
  kind: pattern
  cause: the eslint import/order config ranks index before parent in the type group and puts parent and sibling in separate groups
  resolution: follow the lint message ordering
  artifact: `.` type import should occur before type import of `../graph/types`
  signal: loud
- bun test passed while tsc refused the cast from a guarded Record to LayerDir
  trigger: when copying caller-supplied layer objects through an isRecord type guard
  kind: gotcha
  cause: bun test strips types without checking them; the conversion from { [x: string]: unknown } to LayerDir is refused as a TS2352 cast
  resolution: let the map infer the spread type instead of casting; run check-types, not only bun test
  artifact: error TS2352: Conversion of type '{ [x: string]: unknown; }' to type 'LayerDir' may be a mistake
  signal: loud
- toEqual ignores keys set to undefined, so a result with graph: undefined still matches { config, diagnostics, sources }
  trigger: when a test checks that an optional result key is absent using toEqual on the whole result
  kind: gotcha
  cause: bun's toEqual treats undefined-valued properties as absent; only toStrictEqual or an explicit 'key' in result check notices them
  resolution: check absence with expect('graph' in result).toBe(false), and prove it can fail by temporarily returning graph: undefined
  artifact: expect('graph' in result).toBe(false)
  signal: silent
- built-in readers always win; a loaders entry for .json/.ts/.mts/.mjs/.js is never called
  trigger: when a host passes loaders['.json'] or a loader keyed by a module extension
  kind: pattern
  cause: the plan routes only extensions without a built-in reader to loaders[ext]
  resolution: documented in the module TSDoc and pinned by a test; createLoader may later choose to refuse such keys
  artifact: a loaders entry for .json is not used: JSON is always read built in
  signal: silent
- tsconfig.build.json includes all of src/ except *.test.ts, so fixture .ts files would get declarations emitted into dist/
  trigger: when the acceptance task adds .ts fixtures under src/loader/fixtures/
  kind: location
  cause: the build include is src with only src/**/*.test.ts excluded
  resolution: exclude src/loader/fixtures from tsconfig.build.json (and from lint import/no-unresolved as the plan says) when fixtures land
  artifact: "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
  signal: silent
- the rejection value is a BuildMessage, not an Error instance
  trigger: when import() of a .ts or .mjs file with a syntax error rejects under Bun
  kind: gotcha
  cause: Bun's transpiler reports compile errors as BuildMessage objects
  resolution: read the reason from any object carrying a message property instead of requiring instanceof Error
  artifact: syntax BuildMessage Unexpected ;
  signal: silent
- Bun's module cache returns the first-loaded module, and the very same default object, for a path already imported
  trigger: when a module config path is imported twice in one process after the file changed
  kind: gotcha
  cause: import() of an already-loaded URL is served from the runtime module cache
  resolution: readSource copies each entry when adding $layer instead of mutating it; a test pins the cache behaviour for the README to document
  artifact: cache [{"n":1}] [{"n":1}] true
  signal: silent
- an EACCES assertion can pass vacuously if the process runs as root
  trigger: testing a permission-denied stat path with chmod 000
  kind: pattern
  cause: root ignores directory permissions, so stat succeeds
  resolution: check process.getuid() and branch; confirmed uid 501 here and ran a mutation control that made the test fail
  artifact: a stat failure other than absence is thrown, not read as a missing file
  signal: silent
- import/order puts node: builtins before bun:test, and in a separate group
  trigger: importing bun:test alongside node: builtins in a test file
  kind: gotcha
  cause: the ESLint import/order config treats bun:test as external and node: as builtin
  resolution: put node: imports first, then a blank line, then bun:test
  artifact: `bun:test` import should occur after import of `node:path`
  signal: loud
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: Type 'boolean' is not assignable to type 'number'.
- EdgeTarget.repeat is typed number, so the spec's repeat: true wanted-loop marker is a type error
  trigger: when a test or acceptance fixture typed as Flows writes an edge object with repeat: true
  kind: gotcha
  cause: src/graph/types.ts declares repeat?: number while flatten, GraphEdge and cycles treat repeat: true as the only exemption
  resolution: leave the fixture untyped or widen EdgeTarget.repeat to boolean | number in a scoped task
  artifact: Type 'boolean' is not assignable to type 'number'.
  signal: loud
- besides duplicate-key, the entry that already had that id is also reported unreachable
  trigger: when a fixture writes an inline entry whose generated id already exists in the flow
  kind: gotcha
  cause: flatten drops the losing handler, so nothing leads to the existing entry any more
  resolution: expect the unreachable warning after the duplicate-key
  artifact: warn unreachable flows.next["a.success"]
  signal: loud
- the conflicting entry loses all handlers, so every entry after it is also reported unreachable
  trigger: when a fixture puts a handler-conflict on the flow's start entry
  kind: gotcha
  cause: normalise drops a conflicting entry's handlers for the rest of the pipeline, reachability included
  resolution: put the conflict on an entry that is unreachable anyway when a test wants one diagnostic per stage
  artifact: warn unreachable flows.next.test
  signal: loud
- reachability reads the build result's graph, sources and hooks directly and returns a readonly Diagnostic list
  trigger: when resolveGraph wires reachability in src/graph/index.ts
  kind: location
  cause: Graph carries neither source paths nor hooks
  resolution: call reachability(built) with build's result, beside cycles(built)
  artifact: export function reachability(built: Pick<BuildResult, 'graph' | 'sources' | 'hooks'>): readonly Diagnostic[]
  signal: silent
- only non-implicit nodes are reported as unreachable, because the rule speaks of entries; a reached implicit interactive node is still reported at the handler that named it
  trigger: when an implicit registry-only node hangs off an unreachable entry
  kind: pattern
  cause: an implicit node has no entry, and its source path is its first reference
  resolution: check sources[key].implicit before warning
  artifact: source?.implicit === true
  signal: silent
- build sets FlowSummary.start to empty string, so reachability skips the flow instead of warning about every entry
  trigger: when a flow's $start names a step that is neither an entry nor registered
  kind: gotcha
  cause: build already reports unknown-step at flows.<flow>.$start, and warning about every entry on top of it would bury that error
  resolution: reachability returns no diagnostics for a flow whose start is empty; documented in the module TSDoc and covered by a test
  artifact: if (flow.start === '') {
  signal: silent
- cycles takes Pick<BuildResult, 'graph' | 'sources'> and returns readonly Diagnostic[]; it reads sources for the closing handler's path and ignores hooks
  trigger: when reachability or resolveGraph consumes cycles
  kind: location
  cause: Graph carries no source paths
  resolution: call cycles(built) directly with build's result
  artifact: export function cycles(built: Pick<BuildResult, 'graph' | 'sources'>): readonly Diagnostic[]
  signal: silent
- cycles exempts only repeat === true, but the public EdgeTarget type declares repeat?: number, so the only exempting value is a type error for typed flows
  trigger: when a typed host writes a wanted loop with repeat: true
  kind: gotcha
  cause: types.ts was written before the stage fixed repeat: true as the loop marker
  resolution: left types.ts unchanged as out of scope; the public-surface stage or the spec owner should widen EdgeTarget.repeat to true or boolean | number
  artifact: repeat?: number;
  signal: silent
- the DFS descends the first-declared handler before checking later ones, so a self-loop on a node's second handler is found after the loops deeper down its first handler
  trigger: when ordering expected cycle diagnostics for a node with several handlers
  kind: gotcha
  cause: walk order is depth-first in declaration order, not handler order per node
  resolution: expect cycle diagnostics in DFS order; the test states why
  artifact: cycle flows.next.build.on.fail
  signal: loud
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: function isPlainObject(value: unknown): value is Readonly<Record<string, unknown>>
- artifact: PLAN_TRACKER-rafa-2-open-tomato-define-config.md: repeat?: number;
- FlowSummary.start is empty string because the type requires a string
  trigger: when a flow has no step entry and no resolvable $start
  kind: gotcha
  cause: FlowSummary.start is typed string, not optional
  resolution: documented in the build.ts TSDoc; reachability should treat an empty start as no start
  artifact: start: ''
  signal: silent
- build returns sources (path and implicit flag per node key) and hooks (flow, node, anchor, position) beside the graph; when hooks are not edges
  trigger: when reachability or cycles read build output
  kind: location
  cause: Graph has no field for a when placement or for the path an entry was written at
  resolution: read BuildResult.sources for diagnostic paths and BuildResult.hooks for when anchors
  artifact: export interface GraphHook
  signal: silent
- build makes an implicit node for it, as it does for handler targets, so every hook anchor and edge endpoint is a key of graph.nodes
  trigger: when a when anchor names a registered step that has no entry in the flow
  kind: pattern
  cause: the stage text mentions implicit nodes only for handler targets and defines unknown-step only for an anchor that exists nowhere
  resolution: documented in the module TSDoc; the node's NodeSource is implicit true at the when path, and reachability must not report implicit nodes as unreachable entries
  signal: silent
